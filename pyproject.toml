[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "pandacheck"
version = "0.2.0.dev0"
description = "Portable policy checks for AI-agent configuration."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "ChatPandaAI contributors"}]
dependencies = ["json5>=0.12"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Security",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "build>=1.2", "twine>=5.1"]

[project.scripts]
pandacheck = "pandacheck.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pandacheck"]

[tool.pytest.ini_options]
testpaths = ["tests"]
